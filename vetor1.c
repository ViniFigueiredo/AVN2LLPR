#include <stdio.h>
#include <math.h>
#define TAM 3

int main(){
    int vet[TAM], i, posição, qnt_primos, ordem_primos[100];

    for(i=0;i<TAM;i++){
        scanf("%i",&vet[i]);
    }
    for(i=0;i<TAM;i++){
        printf(" %i ",vet[i]);
    }
    for(i=0;i<TAM;i++){
        if((vet[i]*vet[i]));
    }


    return 0;
}